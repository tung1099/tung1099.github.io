window.POSTS = [
  {
    "id": "thu-tu-thuc-thi-cua-sql",
    "title": "SQL chạy theo thứ tự nào",
    "date": "2026-08-01",
    "category": "Code",
    "topic": "Database & SQL",
    "tags": [
      "SQL",
      "PostgreSQL"
    ],
    "summary": "SELECT viết đầu nhưng chạy gần cuối. Hiểu thứ tự xử lý logic là giải thích được hầu hết lỗi SQL cơ bản — kể cả loại lỗi không báo đỏ, chỉ báo sai số.",
    "body": "Bạn viết `SELECT` đầu tiên, nhưng cơ sở dữ liệu đọc nó gần cuối cùng. Đây là lý do một câu truy vấn trông hoàn toàn hợp lý lại báo lỗi, hoặc tệ hơn — chạy được nhưng trả về kết quả sai.\n\n## Thứ tự viết và thứ tự chạy\n\nThứ tự bạn gõ ra:\n\n```sql\nSELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY ... LIMIT ...\n```\n\nThứ tự SQL xử lý về mặt logic:\n\n![Thứ tự xử lý logic của một câu SELECT](video/UnderstandSQL/understand-sql.mp4)\n\n```mermaid\nflowchart TD\n  F[\"1. FROM / JOIN<br/>dựng tập dữ liệu nguồn\"] --> W[\"2. WHERE<br/>lọc từng dòng\"]\n  W --> G[\"3. GROUP BY<br/>gom thành nhóm\"]\n  G --> H[\"4. HAVING<br/>lọc từng nhóm\"]\n  H --> S[\"5. SELECT<br/>chọn cột, tính alias\"]\n  S --> D[\"6. DISTINCT<br/>bỏ trùng\"]\n  D --> O[\"7. ORDER BY<br/>sắp xếp\"]\n  O --> L[\"8. LIMIT / OFFSET<br/>cắt lấy phần đầu\"]\n  style S stroke-width:3px\n```\n\nChỉ cần nhớ một điều: **`SELECT` chạy sau `WHERE`, `GROUP BY` và `HAVING`, nhưng trước `ORDER BY`.** Gần như mọi thắc mắc về SQL cơ bản đều rơi vào chỗ này.\n\n## Đi qua một ví dụ\n\nTính doanh thu theo khách hàng trong năm 2026, chỉ lấy khách chi trên 10 triệu, xếp từ cao xuống thấp, lấy 5 người đầu:\n\n```sql\nSELECT   c.name,\n         SUM(o.total) AS revenue\nFROM     orders o\nJOIN     customers c ON c.id = o.customer_id\nWHERE    o.created_at >= DATE '2026-01-01'\nGROUP BY c.id, c.name\nHAVING   SUM(o.total) > 10000000\nORDER BY revenue DESC\nLIMIT    5;\n```\n\nMáy chạy như sau:\n\n1. **FROM/JOIN** — ghép `orders` với `customers`, ra một bảng phẳng gồm mọi đơn kèm thông tin khách.\n2. **WHERE** — vứt bỏ mọi dòng có `created_at` trước 2026. Lọc ở mức **từng đơn hàng**.\n3. **GROUP BY** — gom các đơn còn lại theo khách. Sau bước này mỗi khách chỉ còn **một dòng**, các cột chưa được gom bằng hàm tổng hợp đã biến mất.\n4. **HAVING** — vứt bỏ nhóm nào tổng chi không quá 10 triệu. Lọc ở mức **từng nhóm**.\n5. **SELECT** — giờ mới lấy `c.name` và tính `SUM(o.total)`, đặt tên nó là `revenue`. Alias `revenue` **vừa mới ra đời ở bước này**.\n6. **ORDER BY** — sắp xếp. Vì chạy sau `SELECT` nên nó dùng được `revenue`.\n7. **LIMIT** — cắt lấy 5 dòng đầu.\n\n## Bốn hệ quả bạn sẽ gặp trong tuần này\n\n**Alias dùng được ở `ORDER BY` nhưng không dùng được ở `WHERE`.**\n\n```sql\n-- lỗi: column \"revenue\" does not exist\nSELECT SUM(total) AS revenue FROM orders WHERE revenue > 1000;\n\n-- đúng: lặp lại biểu thức, hoặc đẩy vào HAVING / subquery\nSELECT customer_id, SUM(total) AS revenue\nFROM   orders\nGROUP BY customer_id\nHAVING SUM(total) > 1000\nORDER BY revenue DESC;   -- chỗ này alias lại dùng được\n```\n\nKhông phải PostgreSQL khó tính. Lúc `WHERE` chạy thì `revenue` đơn giản là chưa tồn tại.\n\n**`WHERE` và `HAVING` không thay thế cho nhau.** `WHERE` lọc dòng *trước khi* gom, `HAVING` lọc nhóm *sau khi* gom. Đặt sai chỗ thì hoặc là lỗi cú pháp, hoặc là ra số khác:\n\n```sql\n-- lọc đơn năm 2026 rồi mới tính tổng\nWHERE created_at >= DATE '2026-01-01'\n\n-- tính tổng mọi năm rồi mới loại nhóm nhỏ\nHAVING SUM(total) > 10000000\n```\n\nĐổi chỗ hai dòng này là đổi hẳn ý nghĩa câu truy vấn. Đây là loại lỗi không bao giờ báo đỏ, chỉ báo sai số cho sếp.\n\n**`LIMIT 10` không có nghĩa là máy chỉ đọc 10 dòng.** `LIMIT` là bước cuối. Trước nó, database vẫn phải quét, gom nhóm và sắp xếp toàn bộ dữ liệu thoả `WHERE`. Câu `ORDER BY revenue DESC LIMIT 10` trên bảng 50 triệu dòng vẫn có thể tốn vài giây, và đó cũng là lý do phân trang bằng `OFFSET` lớn càng lúc càng chậm.\n\n**Điều kiện của `LEFT JOIN` đặt vào `WHERE` sẽ giết luôn phần `LEFT`.**\n\n```sql\n-- tưởng là \"mọi khách, kèm đơn năm 2026 nếu có\"\n-- thực tế là INNER JOIN: khách không có đơn nào bị loại sạch\nSELECT c.name, o.id\nFROM   customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nWHERE  o.created_at >= DATE '2026-01-01';\n\n-- đúng: điều kiện thuộc về phép JOIN thì để trong ON\nSELECT c.name, o.id\nFROM   customers c\nLEFT JOIN orders o\n       ON o.customer_id = c.id\n      AND o.created_at >= DATE '2026-01-01';\n```\n\nLý do vẫn là thứ tự: `LEFT JOIN` sinh ra các dòng có `o.*` toàn `NULL`, rồi `WHERE` chạy sau và loại hết chúng vì `NULL >= '2026-01-01'` không đúng.\n\n## Một lưu ý quan trọng: đây là thứ tự *logic*\n\nThứ tự trên là thứ tự SQL **định nghĩa ra kết quả**, không phải các bước máy thực sự làm. Bộ tối ưu hoàn toàn được phép sắp xếp lại, miễn là kết quả không đổi: đẩy điều kiện lọc xuống dưới phép join, dừng sớm khi đã đủ số dòng cho `LIMIT` nhờ index, chuyển subquery thành join.\n\nNói cách khác, thứ tự logic giúp bạn trả lời \"câu này ra kết quả gì\". Muốn biết \"câu này chạy thế nào\" thì phải đọc `EXPLAIN` — tôi viết kỹ hơn ở bài [đã tạo index rồi mà query vẫn chậm](#/post/index-tao-roi-van-cham).\n\n## Ghi nhớ trong một dòng\n\n> `FROM` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `DISTINCT` → `ORDER BY` → `LIMIT`\n\nBốn lỗi ở trên đều là hệ quả trực tiếp của một dòng này. Học thuộc nó rẻ hơn nhiều so với việc debug một báo cáo sai số.\n"
  },
  {
    "id": "access-token-va-refresh-token",
    "title": "Access token và refresh token: vì sao phải có hai cái",
    "date": "2026-08-01",
    "category": "Code",
    "topic": "Bảo mật",
    "tags": [
      "Bảo mật",
      "JWT",
      "Spring"
    ],
    "summary": "Một token không giải được mâu thuẫn giữa \"sống lâu cho tiện\" và \"thu hồi được cho an toàn\". Hai token, mỗi cái nhận một nửa bài toán.",
    "body": "Câu hỏi hay gặp nhất khi mới làm xác thực: đã có token rồi, sao còn phải thêm một cái token nữa cho rối?\n\nCâu trả lời nằm ở một mâu thuẫn không giải được bằng một token duy nhất.\n\n## Mâu thuẫn: sống lâu hay thu hồi được\n\nJWT là **stateless**. Server không lưu gì cả, chỉ ký rồi kiểm tra chữ ký. Đó chính là điểm mạnh — mọi pod đều xác thực được mà không cần hỏi database. Nhưng nó kéo theo một hệ quả khó chịu: **token đã phát ra thì không thu hồi được**. Nó có hiệu lực cho tới lúc hết hạn, kể cả khi người dùng đã bấm đăng xuất, đã đổi mật khẩu, hoặc bạn vừa phát hiện máy họ bị nhiễm mã độc.\n\nThế nên bạn kẹt giữa hai lựa chọn:\n\n- **Hạn dài (30 ngày)** — người dùng không phải đăng nhập lại, nhưng token rò rỉ là kẻ tấn công dùng được nguyên tháng.\n- **Hạn ngắn (10 phút)** — rò rỉ thì thiệt hại nhỏ, nhưng cứ 10 phút lại bắt người ta gõ mật khẩu thì không ai chịu nổi.\n\nHai token sinh ra để tách hai yêu cầu này ra, mỗi cái giải một nửa.\n\n![Access token và refresh token phối hợp thế nào](video/AccessanAndRefreshToken/access-refresh-token.mp4)\n\n## Mỗi cái làm một việc\n\n| | Access token | Refresh token |\n|---|---|---|\n| Sống bao lâu | 5–15 phút | vài ngày tới vài tuần |\n| Gửi kèm khi nào | **mọi** request tới API | chỉ khi gọi `/auth/refresh` |\n| Server có lưu không | không (stateless) | **có** — lưu để thu hồi được |\n| Rò rỉ thì sao | hết hạn rất nhanh | thu hồi ngay được |\n| Chứa gì | userId, roles, scope | gần như không gì, chỉ là một mã tra cứu |\n\nAccess token đi khắp nơi nên phải sống ngắn. Refresh token sống lâu nên phải đi ít chỗ nhất có thể, và phải nằm trong tầm kiểm soát của server.\n\n```mermaid\nsequenceDiagram\n  participant C as Client\n  participant A as Auth Service\n  participant R as API\n  C->>A: đăng nhập (email + mật khẩu)\n  A-->>C: access (10 phút) + refresh (14 ngày)\n  C->>R: GET /orders + access token\n  R-->>C: 200 OK\n  Note over C,R: ...10 phút sau...\n  C->>R: GET /orders + access token\n  R-->>C: 401 token hết hạn\n  C->>A: POST /auth/refresh + refresh token\n  A-->>C: access mới + refresh mới\n  C->>R: GET /orders + access token mới\n  R-->>C: 200 OK\n```\n\nĐiểm quan trọng: **người dùng không thấy gì cả**. Toàn bộ vòng lặp này nằm trong một interceptor phía client.\n\n## Viết bằng Spring\n\nPhần phát token, lúc đăng nhập:\n\n```java\n@Service\n@RequiredArgsConstructor\npublic class AuthService {\n    private final JwtEncoder jwt;\n    private final RefreshTokenRepository refreshTokens;\n\n    public TokenPair login(String email, String rawPassword) {\n        User user = authenticate(email, rawPassword);\n        return new TokenPair(newAccessToken(user), newRefreshToken(user));\n    }\n\n    private String newAccessToken(User user) {\n        JwtClaimsSet claims = JwtClaimsSet.builder()\n            .subject(user.getId().toString())\n            .claim(\"roles\", user.getRoles())\n            .issuedAt(Instant.now())\n            .expiresAt(Instant.now().plus(10, ChronoUnit.MINUTES))\n            .build();\n        return jwt.encode(JwtEncoderParameters.from(claims)).getTokenValue();\n    }\n\n    /* Refresh token KHÔNG phải JWT: chỉ là chuỗi ngẫu nhiên, lưu bản băm\n       trong database để thu hồi được. */\n    private String newRefreshToken(User user) {\n        byte[] raw = new byte[32];\n        new SecureRandom().nextBytes(raw);\n        String token = Base64.getUrlEncoder().withoutPadding().encodeToString(raw);\n        refreshTokens.save(new RefreshToken(\n            sha256(token), user.getId(), Instant.now().plus(14, ChronoUnit.DAYS)));\n        return token;\n    }\n}\n```\n\nPhần đổi token, có **xoay vòng** (rotation) — mỗi lần refresh là cấp cái mới và huỷ cái cũ:\n\n```java\n@Transactional\npublic TokenPair refresh(String presented) {\n    RefreshToken stored = refreshTokens.findByHash(sha256(presented))\n        .orElseThrow(() -> new BadCredentialsException(\"refresh token không hợp lệ\"));\n\n    if (stored.isRevoked()) {\n        // Token đã dùng rồi mà còn xuất hiện lần nữa: gần như chắc chắn bị đánh cắp.\n        // Huỷ toàn bộ phiên của người dùng này.\n        refreshTokens.revokeAllForUser(stored.getUserId());\n        throw new BadCredentialsException(\"phát hiện tái sử dụng refresh token\");\n    }\n    if (stored.getExpiresAt().isBefore(Instant.now())) {\n        throw new CredentialsExpiredException(\"refresh token hết hạn\");\n    }\n\n    stored.revoke();\n    User user = users.findById(stored.getUserId()).orElseThrow();\n    return new TokenPair(newAccessToken(user), newRefreshToken(user));\n}\n```\n\nCái nhánh `isRevoked()` ở trên là phần đắt giá nhất của rotation, và cũng là phần hay bị bỏ. Nó gọi là **reuse detection**: refresh token dùng một lần rồi bỏ, nên nếu một token đã bị huỷ lại quay lại lần nữa, nghĩa là có hai bên đang giữ cùng một token — người dùng thật và kẻ trộm. Không phân biệt được ai là ai, cách an toàn nhất là đá cả hai ra.\n\n## Lưu token ở đâu phía client\n\nĐây là chỗ tranh cãi nhiều nhất, nhưng đánh đổi thì rõ ràng:\n\n- **`localStorage`** — dễ viết, JavaScript đọc được. Nghĩa là **chỉ cần một lỗ XSS là mất token**. Dùng cho access token ngắn hạn thì tạm chấp nhận; để refresh token ở đây là tự bắn vào chân.\n- **Cookie `HttpOnly` + `Secure` + `SameSite=Strict`** — JavaScript không đọc được nên XSS không lấy được. Đổi lại phải xử lý CSRF, nhưng `SameSite=Strict` đã chặn gần hết. Đây là chỗ đúng cho **refresh token**, và đặt `Path=/auth/refresh` để nó không bị gửi kèm trong mọi request khác.\n\nCấu hình thường dùng: access token giữ trong bộ nhớ của ứng dụng (biến JavaScript, mất khi F5 cũng không sao vì refresh được), refresh token nằm trong cookie `HttpOnly`.\n\n## Bốn lỗi hay gặp\n\n**Access token sống 30 ngày.** Lúc này refresh token chỉ còn là đồ trang trí — bạn đã mất toàn bộ lợi ích và chỉ giữ lại phần phức tạp.\n\n**Refresh token cũng là JWT stateless.** Nếu server không lưu gì thì đăng xuất không có ý nghĩa, và reuse detection cũng không làm được. Refresh token **bắt buộc** phải có trạng thái ở server.\n\n**Nhiều request cùng gặp 401 một lúc.** Trang vừa mở gọi năm API song song, cả năm cùng hết hạn, cả năm cùng gọi `/refresh`. Với rotation thì cái đầu tiên thành công, bốn cái sau trình ra token đã bị huỷ và bạn vừa tự đá người dùng ra ngoài. Cách xử lý: phía client dồn về **một lời gọi refresh duy nhất**, các request còn lại xếp hàng chờ kết quả của nó.\n\n**Đăng xuất chỉ xoá token ở client.** Refresh token vẫn còn hiệu lực trong database. Đăng xuất phải là một lời gọi API để server đánh dấu thu hồi.\n\n## Khi nào không cần\n\nNếu ứng dụng của bạn là một server render HTML truyền thống, **session cookie vẫn ổn** — thậm chí đơn giản hơn và thu hồi dễ hơn. Cặp access/refresh sinh ra cho tình huống có nhiều client (web, mobile, dịch vụ khác) gọi vào một API stateless, nơi việc chia sẻ session giữa các server là thứ bạn muốn tránh.\n\nĐừng thêm nó chỉ vì thấy chỗ khác dùng.\n"
  },
  {
    "id": "spring-component-service-repository",
    "title": "@Component, @Service, @Repository khác nhau chỗ nào?",
    "date": "2026-07-31",
    "category": "Code",
    "topic": "Spring",
    "tags": [
      "Java",
      "Spring"
    ],
    "summary": "Câu trả lời ngắn: về mặt đăng ký bean thì gần như không. Nhưng có một khác biệt thật sự, và nó quan trọng hơn bạn nghĩ.",
    "body": "Đây là câu hỏi phỏng vấn kinh điển, và cũng là chỗ nhiều người trả lời nửa vời.\n\n## Câu trả lời ngắn\n\n`@Service` và `@Controller` là **meta-annotation** của `@Component`. Mở mã nguồn Spring ra sẽ thấy:\n\n```java\n@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\n@Component                       // <-- ngay đây\npublic @interface Service {\n    @AliasFor(annotation = Component.class)\n    String value() default \"\";\n}\n```\n\nNghĩa là với cơ chế component scanning, cả ba đều cho ra một bean giống hệt nhau. Đổi `@Service` thành `@Component` thì ứng dụng vẫn chạy bình thường.\n\n## Nhưng @Repository thì khác thật\n\nĐây là chỗ nhiều người bỏ sót. `@Repository` kích hoạt một **post-processor** tên là `PersistenceExceptionTranslationPostProcessor`. Nó bọc bean lại bằng proxy và dịch exception đặc thù của tầng persistence sang cây `DataAccessException` của Spring.\n\nCụ thể:\n\n```java\n// Không có @Repository\ncatch (SQLIntegrityConstraintViolationException e) { }   // gắn chặt vào JDBC\ncatch (ConstraintViolationException e) { }               // gắn chặt vào Hibernate\n\n// Có @Repository\ncatch (DataIntegrityViolationException e) { }            // độc lập nhà cung cấp\n```\n\nĐổi từ Hibernate sang JOOQ, tầng service không phải sửa dòng nào. Đó là toàn bộ lý do `@Repository` tồn tại.\n\n> Lưu ý: nếu bạn dùng Spring Data JPA và extend `JpaRepository`, việc dịch exception đã được bật sẵn. Bạn thậm chí không cần ghi `@Repository`.\n\n## Vậy tại sao vẫn nên phân biệt?\n\nBa lý do, không cái nào liên quan đến runtime.\n\n**1. Đọc code nhanh hơn.** Nhìn annotation là biết class này thuộc tầng nào, không cần đọc nội dung.\n\n**2. Bắt lỗi kiến trúc tự động.** Với ArchUnit, bạn viết được luật kiểu:\n\n```java\n@ArchTest\nstatic final ArchRule controller_khong_goi_thang_repository =\n    noClasses().that().areAnnotatedWith(Controller.class)\n        .should().dependOnClassesThat().areAnnotatedWith(Repository.class);\n```\n\nLuật này chỉ chạy được khi annotation phản ánh đúng vai trò.\n\n**3. AOP theo tầng.** Muốn đo thời gian thực thi của mọi method trong tầng service:\n\n```java\n@Around(\"@within(org.springframework.stereotype.Service)\")\npublic Object doThoiGian(ProceedingJoinPoint jp) throws Throwable {\n    long t = System.nanoTime();\n    try {\n        return jp.proceed();\n    } finally {\n        log.info(\"{} mất {} ms\", jp.getSignature(), (System.nanoTime() - t) / 1_000_000);\n    }\n}\n```\n\nNếu mọi class đều là `@Component`, pointcut này vô nghĩa.\n\n## Tóm lại\n\n| Annotation | Khác biệt kỹ thuật | Dùng cho |\n|---|---|---|\n| `@Component` | Bean thường | Class không thuộc tầng nào rõ ràng |\n| `@Service` | Không có | Logic nghiệp vụ |\n| `@Repository` | Dịch exception persistence | Class truy cập dữ liệu |\n| `@Controller` | Được quét bởi handler mapping | Đầu vào HTTP |\n\nCâu trả lời hay cho câu hỏi phỏng vấn này không phải \"chúng giống nhau\" hay \"chúng khác nhau\", mà là: **giống nhau về đăng ký bean, khác nhau về ý định — và riêng `@Repository` có thêm chức năng thật.**"
  },
  {
    "id": "rag-cho-nguoi-viet-java",
    "title": "RAG giải thích cho người viết Java",
    "date": "2026-07-28",
    "category": "Code",
    "topic": "AI ứng dụng",
    "tags": [
      "AI",
      "Java",
      "Spring"
    ],
    "summary": "Bỏ qua phần sơ đồ. RAG chỉ là tìm kiếm cộng nối chuỗi — và hầu hết vấn đề nằm ở bước tìm kiếm.",
    "body": "Mỗi lần đọc bài về RAG tôi lại thấy một sơ đồ đầy mũi tên. Thật ra nó đơn giản hơn thế nhiều.\n\n## RAG là gì, nói bằng ngôn ngữ backend\n\nMô hình ngôn ngữ chỉ biết những gì có trong dữ liệu huấn luyện. Nó không biết tài liệu nội bộ công ty bạn. RAG giải quyết bằng cách làm đúng một việc:\n\n**Trước khi hỏi mô hình, đi tìm vài đoạn văn bản liên quan rồi dán vào prompt.**\n\nHết. Viết bằng mã giả:\n\n```java\nString traLoi(String cauHoi) {\n    List<String> doanLienQuan = timKiem(cauHoi, 5);      // Retrieval\n    String prompt = xayPrompt(doanLienQuan, cauHoi);\n    return llm.goi(prompt);                              // Generation\n}\n```\n\nToàn bộ độ phức tạp của RAG nằm trong hàm `timKiem`.\n\n## Vì sao không dùng LIKE\n\nVì người dùng hỏi *\"làm sao xin nghỉ phép\"* còn tài liệu viết *\"quy trình đăng ký ngày nghỉ có lương\"*. Không có từ nào trùng nhau.\n\nGiải pháp là **embedding**: biến mỗi đoạn văn thành một vector vài trăm chiều, sao cho hai đoạn cùng ý nghĩa thì hai vector nằm gần nhau. Tìm kiếm khi đó là tìm vector gần nhất theo cosine similarity.\n\n## Bước indexing — làm một lần\n\n1. **Đọc tài liệu** — PDF, Word, Confluence, bất cứ gì\n2. **Cắt nhỏ (chunking)** — thường 500–1000 token mỗi đoạn, chồng lấn 10–15%\n3. **Tạo embedding** cho từng đoạn\n4. **Lưu vào vector store** — pgvector, Qdrant, Elasticsearch đều được\n\nVới Spring AI, phần này gọn đáng ngạc nhiên:\n\n```java\n@Bean\nApplicationRunner napTaiLieu(VectorStore store) {\n    return args -> {\n        var reader = new TikaDocumentReader(\"classpath:so-tay-nhan-vien.pdf\");\n        var splitter = new TokenTextSplitter();\n        store.add(splitter.apply(reader.get()));\n    };\n}\n```\n\n## Bước truy vấn — mỗi lần người dùng hỏi\n\n```java\n@Service\npublic class HoiDapService {\n\n    private final ChatClient chat;\n    private final VectorStore store;\n\n    public String hoi(String cauHoi) {\n        return chat.prompt()\n            .advisors(new QuestionAnswerAdvisor(store,\n                SearchRequest.defaults()\n                    .withTopK(5)\n                    .withSimilarityThreshold(0.7)))\n            .user(cauHoi)\n            .call()\n            .content();\n    }\n}\n```\n\n`QuestionAnswerAdvisor` làm hộ bạn việc tìm kiếm và chèn vào prompt.\n\n## Chỗ thường hỏng\n\nTôi đã dựng thứ này vài lần. Lỗi gần như luôn nằm ở khâu retrieval, không phải ở mô hình.\n\n**Chunk cắt sai chỗ.** Cắt cứng theo số ký tự sẽ chặt đôi một bảng, hoặc tách tiêu đề khỏi nội dung của nó. Cắt theo cấu trúc tài liệu cho kết quả tốt hơn hẳn.\n\n**topK quá lớn.** Nhồi 20 đoạn vào prompt không làm câu trả lời tốt hơn, chỉ làm mô hình rối và tốn tiền. Bắt đầu từ 3–5.\n\n**Không đặt ngưỡng similarity.** Nếu không có tài liệu nào thật sự liên quan, hệ thống vẫn trả về 5 đoạn gần nhất dù chúng chẳng ăn nhập gì. Mô hình sẽ cố bịa ra câu trả lời từ rác.\n\n**Chỉ dùng vector search.** Vector kém khi gặp mã sản phẩm, tên riêng, số hiệu văn bản — những thứ cần khớp chính xác. Kết hợp cả BM25 và vector, gọi là hybrid search, thường tăng chất lượng rõ rệt.\n\n## Khi nào đừng dùng RAG\n\n- **Tài liệu nhỏ hơn cửa sổ ngữ cảnh** — dán thẳng vào prompt, đơn giản hơn nhiều\n- **Dữ liệu có cấu trúc** — câu hỏi kiểu \"doanh thu quý 2 bao nhiêu\" cần text-to-SQL, không phải RAG\n- **Cần độ chính xác tuyệt đối** — RAG giảm ảo giác chứ không loại bỏ được\n\n## Đọc thêm\n\nTài liệu [Spring AI](https://docs.spring.io/spring-ai/reference/) khá đầy đủ, phần RAG có ví dụ chạy được ngay."
  },
  {
    "id": "pub-sub-va-event-driven",
    "title": "Pub/Sub: vì sao Order Service không nên gọi thẳng Email Service",
    "date": "2026-07-26",
    "category": "Code",
    "topic": "Kafka",
    "tags": [
      "Kafka",
      "Hệ phân tán",
      "Kiến trúc"
    ],
    "summary": "Gọi trực tiếp thì Email chết là cả luồng đặt hàng chết theo. Bài này đi từ vấn đề đó tới Pub/Sub, rồi tới chỗ Kafka làm khác mọi người tưởng: nó không có Queue và Topic riêng.",
    "body": "Sai lầm hay gặp nhất khi tách microservice là tách được về mặt tiến trình nhưng vẫn buộc chặt về mặt gọi hàm. Service A gọi HTTP sang service B, chờ B trả lời, rồi mới trả lời người dùng. Về bản chất đó vẫn là một hàm gọi hàm, chỉ khác là giờ nó đi qua dây mạng và có thêm chừng năm kiểu hỏng mới.\n\n## Vấn đề: gọi trực tiếp\n\nKhách bấm \"Đặt hàng\". `OrderService` phải làm bốn việc: lưu đơn, xuất hoá đơn, trừ tồn kho, gửi email xác nhận.\n\n```mermaid\nflowchart LR\n  U[\"Khách hàng\"] --> O[\"Order Service\"]\n  O --> I[\"Invoice\"]\n  O --> S[\"Inventory\"]\n  O --> E[\"Email\"]\n  O -. thêm sau .-> R[\"Báo cáo doanh thu\"]\n  style E stroke-width:3px\n  style R stroke-dasharray: 5 5\n```\n\nBa hệ quả, cái nào cũng khó chịu:\n\n**Chậm nhất quyết định tất cả.** Thời gian phản hồi của API đặt hàng bằng tổng thời gian của cả bốn lời gọi. Email SMTP chậm 800ms thì khách chờ thêm 800ms, dù họ chẳng quan tâm email.\n\n**Yếu nhất quyết định tất cả.** Email service chết thì `OrderService` nhận timeout. Giờ bạn phải chọn: trả lỗi cho khách dù đơn đã lưu thành công, hay nuốt lỗi và mất email? Cả hai đều sai.\n\n**Thêm việc là phải sửa code cũ.** Sếp muốn có báo cáo doanh thu theo thời gian thực. Bạn mở `OrderService` ra, thêm một lời gọi nữa, deploy lại cái service quan trọng nhất hệ thống — chỉ để thêm một thứ không liên quan gì tới việc đặt hàng.\n\n## Pub/Sub: đảo ngược chiều phụ thuộc\n\nThay vì gọi thẳng, `OrderService` chỉ **công bố một sự kiện**: \"đơn hàng `#1234` vừa được tạo\". Nó đẩy lên broker rồi đi làm việc khác. Không biết ai nghe, không biết có bao nhiêu người nghe, không quan tâm họ xử lý xong chưa.\n\n![Luồng Pub/Sub: từ gọi trực tiếp sang publish qua broker — video của **@duckmink_nguyen**](video/pub_sub/pub-sub.mp4)\n\n```mermaid\nflowchart LR\n  O[\"Order Service<br/>publisher\"] -->|\"OrderCreated\"| B((\"Broker<br/>topic: order.created\"))\n  B --> I[\"Invoice\"]\n  B --> S[\"Inventory\"]\n  B --> E[\"Email\"]\n  B --> R[\"Báo cáo doanh thu<br/>(thêm sau, không sửa Order)\"]\n  style B stroke-width:3px\n```\n\nĐổi lại được ba thứ đúng với ba vấn đề ở trên:\n\n- API đặt hàng giờ chỉ tốn thời gian lưu đơn + đẩy một message. Phần còn lại chạy nền.\n- Email service chết cũng không ai biết. Broker giữ message lại, lúc nó sống dậy thì đọc tiếp từ chỗ đang dở.\n- Thêm consumer mới **không phải đụng một dòng nào** trong `OrderService`.\n\n> Điều đánh đổi: hệ thống trở nên **nhất quán sau cùng**. Đơn đã tạo xong nhưng email có thể tới sau vài giây, tồn kho có thể trừ sau vài trăm mili giây. Nếu nghiệp vụ của bạn không chịu được độ trễ đó thì Pub/Sub không phải câu trả lời — hãy dùng transaction.\n\n## Queue và Topic khác nhau chỗ nào\n\nĐây là chỗ hay nhầm nhất, và cách phân biệt kinh điển là:\n\n- **Queue** — mỗi message chỉ **một** consumer xử lý. Như bốc số ở ngân hàng: một số, một giao dịch viên.\n- **Topic** — mỗi message tới **tất cả** subscriber đã đăng ký. Như phát thanh: ai mở đúng kênh đều nghe cùng nội dung.\n\nVới RabbitMQ thì đúng là hai thứ khác nhau thật: bạn khai báo queue, khai báo exchange, rồi buộc chúng lại.\n\n## Nhưng Kafka không có hai thứ đó\n\nKafka **chỉ có topic**. Không có kiểu object nào tên là queue. Cái quyết định message đi tới một người hay tất cả mọi người là **`group.id` của consumer**:\n\n```mermaid\nflowchart LR\n  T[\"topic: order.created<br/>4 partition\"]\n  T --> G1\n  T --> G2\n  subgraph G1[\"group.id = email-service\"]\n    C1[\"pod 1\"]\n    C2[\"pod 2\"]\n  end\n  subgraph G2[\"group.id = invoice-service\"]\n    C3[\"pod 1\"]\n  end\n  style G1 stroke-width:3px\n  style G2 stroke-width:3px\n```\n\nQuy tắc chỉ có hai dòng:\n\n1. **Cùng một `group.id`** → Kafka chia partition cho các thành viên, mỗi message chỉ **một** pod trong nhóm nhận. Đây chính là ngữ nghĩa **queue**, và cũng là cách bạn scale ngang.\n2. **Khác `group.id`** → mỗi nhóm nhận **đầy đủ** toàn bộ message, độc lập offset với nhau. Đây chính là ngữ nghĩa **pub/sub**.\n\nNghĩa là cùng một topic `order.created`, `email-service` chạy 2 pod vẫn chỉ gửi một email cho mỗi đơn, trong khi `invoice-service` vẫn nhận được đủ mọi đơn. Bạn không phải chọn queue hay topic lúc thiết kế — bạn chọn lúc đặt tên nhóm.\n\nLỗi kinh điển của người mới: mỗi pod tự sinh một `group.id` ngẫu nhiên lúc khởi động. Kết quả là chạy 3 pod thì khách nhận 3 email cho một đơn hàng.\n\n## Viết bằng Spring Kafka\n\nBên publish, không biết gì về người nhận:\n\n```java\n@Service\n@RequiredArgsConstructor\npublic class OrderService {\n    private final KafkaTemplate<String, OrderCreated> kafka;\n\n    @Transactional\n    public Order create(OrderRequest req) {\n        Order order = repository.save(Order.from(req));\n        // key = orderId để mọi sự kiện của cùng một đơn vào cùng một partition,\n        // nhờ đó giữ đúng thứ tự\n        kafka.send(\"order.created\", order.getId().toString(),\n                   new OrderCreated(order.getId(), order.getTotal()));\n        return order;\n    }\n}\n```\n\nBên nhận, hai nhóm khác nhau nên cả hai đều nhận đủ:\n\n```java\n@KafkaListener(topics = \"order.created\", groupId = \"email-service\")\npublic void sendConfirmation(OrderCreated event) {\n    mailer.sendOrderConfirmation(event.orderId());\n}\n\n@KafkaListener(topics = \"order.created\", groupId = \"invoice-service\")\npublic void issueInvoice(OrderCreated event) {\n    invoiceService.create(event.orderId());\n}\n```\n\n```properties\nspring.kafka.consumer.auto-offset-reset=earliest\nspring.kafka.consumer.enable-auto-commit=false\nspring.kafka.listener.ack-mode=manual\n```\n\n## Ba thứ Pub/Sub không cho bạn miễn phí\n\nĐây là phần các bài giới thiệu hay bỏ qua, và cũng là phần khiến người ta vỡ mộng lúc lên production.\n\n**Thứ tự chỉ được đảm bảo trong một partition.** Muốn mọi sự kiện của đơn `#1234` đúng thứ tự thì phải dùng `orderId` làm key, như đoạn code trên. Không có key thì message rải đều các partition và thứ tự mất ngay.\n\n**Giao hàng là at-least-once, không phải exactly-once.** Sớm muộn một message sẽ được xử lý hai lần. Consumer phải chịu được điều đó — tôi viết kỹ chuyện này ở bài về rebalance.\n\n**Ghi database và bắn event không nằm trong cùng một transaction.** Đoạn code `create()` ở trên vẫn còn lỗ hổng: nếu `save()` xong mà process chết trước khi `send()` kịp đi, đơn hàng tồn tại nhưng không ai biết. Lời giải là **outbox pattern** — ghi event vào một bảng trong cùng transaction, rồi có tiến trình riêng đọc bảng đó đẩy lên Kafka.\n\n## Khi nào thì đừng dùng\n\n- Người gọi **cần kết quả ngay** để trả lời người dùng (kiểm tra thẻ, xác thực) — cứ gọi trực tiếp\n- Hệ thống chỉ có hai ba service và không định lớn thêm — thêm broker là thêm một thứ nữa phải trực\n- Nghiệp vụ **bắt buộc nhất quán mạnh** — tiền bạc trong cùng một ngữ cảnh thì dùng transaction database\n\nPub/Sub giải quyết bài toán ghép nối lỏng và mở rộng, đổi lại bằng độ phức tạp vận hành và tính nhất quán sau cùng. Nó là công cụ, không phải huy chương.\n\n---\n\n*Video minh hoạ trong bài do **@duckmink_nguyen** thực hiện.*\n"
  },
  {
    "id": "viet-de-nghi-cho-ro",
    "title": "Tôi viết blog vì trí nhớ mình tệ",
    "date": "2026-07-25",
    "category": "Cuộc sống",
    "tags": [
      "Ghi chép",
      "Nghề nghiệp"
    ],
    "summary": "Sáu tháng sau, thứ duy nhất còn lại của một buổi debug bốn tiếng là cảm giác mơ hồ rằng mình từng biết cách sửa.",
    "body": "Tuần trước tôi gặp một lỗi `LazyInitializationException`. Mất khoảng bốn mươi phút để sửa.\n\nRồi tôi nhớ ra: hai năm trước tôi đã gặp đúng lỗi này. Cũng mất chừng đó thời gian. Cũng đi qua đúng những ngõ cụt đó.\n\nBốn mươi phút, trả hai lần.\n\n## Điều tôi hiểu muộn\n\nKinh nghiệm không tự tích lại. Nó bay hơi, trừ khi bạn viết nó xuống.\n\nTôi từng nghĩ viết blog là chuyện của những người đã giỏi, đã có gì đó để dạy. Thật ra ngược lại. Người ta không viết vì đã hiểu — người ta hiểu vì đã viết.\n\nCó một bài kiểm tra rất tàn nhẫn: thử giải thích thứ bạn vừa sửa cho một người không biết gì về nó. Trong đầu, lời giải thích trôi rất mượt. Gõ ra thành câu, chỗ hổng lộ ngay ở dòng thứ ba.\n\n> Bạn không biết mình hiểu tới đâu cho đến khi phải viết nó ra.\n\n## Ba thứ đã thay đổi\n\n**Tôi đọc kỹ hơn.** Khi biết mình sẽ viết lại, tôi không còn lướt tài liệu nữa. Đọc để dùng khác đọc để chép.\n\n**Tôi bớt tự tin một cách lành mạnh.** Nhiều lần đang viết, tôi phát hiện mình chưa hiểu thứ mình tưởng đã hiểu. Thà phát hiện lúc đó còn hơn lúc đang giải thích cho đồng nghiệp.\n\n**Tôi có chỗ để chỉ tới.** Ai đó hỏi một câu tôi đã viết rồi — gửi link. Không phải vì lười, mà vì bài viết bao giờ cũng đầy đủ hơn câu trả lời ứng khẩu.\n\n## Ghi thế nào để sáu tháng sau còn dùng được\n\nGhi chép chết vì ba lý do, tôi đều đã mắc cả ba.\n\n**Chép nguyên văn.** Cảm giác rất năng suất — tay bận, trang giấy đầy — nhưng não gần như không tham gia. Nếu không diễn đạt lại được bằng từ của mình thì tức là chưa hiểu.\n\n**Ôm quá nhiều ý trong một ghi chép.** Ghi chép dài dòng không bao giờ được đọc lại. Một ghi chép, một ý, một tiêu đề rõ ràng.\n\n**Không ghi bối cảnh.** Đây là lỗi lớn nhất. Ghi chép nào cũng nên trả lời được câu *\"tôi sẽ cần thứ này lúc nào\"*:\n\n```text\nÝ:        Tắt spring.jpa.open-in-view, rồi sửa mọi chỗ vỡ.\nDùng khi: Gặp LazyInitializationException, hoặc query lạ chạy ở tầng view.\n```\n\nDòng thứ hai mới là dòng giúp bạn tìm lại được nó.\n\n## Vậy nên có trang này\n\nKhông có lịch đăng. Không có mục tiêu lượt xem. Chủ yếu là một cái kho cho chính tôi, để lần thứ ba gặp `LazyInitializationException` thì mất bốn phút thay vì bốn mươi.\n\nNếu tình cờ nó tiết kiệm được thời gian cho bạn nữa thì tốt quá."
  },
  {
    "id": "hashmap-ben-trong",
    "title": "HashMap chạy thế nào bên trong",
    "date": "2026-07-24",
    "category": "Code",
    "topic": "Java core",
    "tags": [
      "Java",
      "Collections"
    ],
    "summary": "Bucket, va chạm, cây đỏ đen và vì sao quên override hashCode là hỏng. Hiểu chỗ này thì nửa số câu hỏi phỏng vấn về Collections tự trả lời được.",
    "body": "`HashMap` là thứ ai cũng dùng từ tuần đầu đi làm, nhưng hỏi \"nó tìm phần tử nhanh bằng cách nào\" thì nhiều người kẹt. Bài này mổ đúng phần bên trong.\n\n## Ý tưởng gốc: mảng + hàm băm\n\nBên trong `HashMap` là một mảng, Java gọi là `table`. Mỗi ô trong mảng gọi là một **bucket**. Khi bạn `put(key, value)`, nó không đi tìm chỗ trống — nó **tính ra** chỗ phải nằm:\n\n```java\n// rút gọn từ mã nguồn JDK\nint h = key.hashCode();\nh = h ^ (h >>> 16);              // trộn 16 bit cao xuống thấp\nint index = (table.length - 1) & h;   // thay cho phép chia lấy dư\n```\n\nVì `table.length` luôn là luỹ thừa của 2, phép `& (n-1)` tương đương `% n` nhưng nhanh hơn nhiều. Dòng `h ^ (h >>> 16)` tồn tại vì nếu chỉ lấy vài bit thấp, những `hashCode` khác nhau ở bit cao sẽ dồn hết vào cùng một bucket.\n\n```mermaid\nflowchart TD\n  A[\"put(key, value)\"] --> B[\"tính hash(key)\"]\n  B --> C[\"index = (n-1) & hash\"]\n  C --> D{\"bucket rỗng?\"}\n  D -- có --> E[\"đặt node vào\"]\n  D -- không --> F{\"key đã tồn tại?\"}\n  F -- có --> G[\"ghi đè value\"]\n  F -- không --> H[\"nối vào cuối danh sách\"]\n  H --> I{\"danh sách >= 8 phần tử<br/>và table >= 64?\"}\n  I -- có --> J[\"chuyển thành cây đỏ đen\"]\n  I -- không --> K[\"giữ nguyên danh sách liên kết\"]\n```\n\n## Va chạm: danh sách liên kết, rồi cây\n\nHai key khác nhau hoàn toàn có thể ra cùng một index. Đó là **va chạm**, và nó bình thường. Khi đó `HashMap` nối chúng thành một danh sách liên kết trong cùng bucket.\n\nVấn đề là danh sách dài thì tra cứu tụt về `O(n)`. Từ Java 8, khi một bucket có từ **8** phần tử trở lên *và* `table` đã đủ 64 ô, bucket đó được chuyển thành **cây đỏ đen** — tra cứu trở lại `O(log n)`. Khi số phần tử tụt xuống 6, nó quay lại danh sách.\n\nCon số 8 không phải chọn bừa. Với hàm băm phân bố tốt, xác suất một bucket có 8 phần tử là khoảng 0.00000006 — tức là nếu bạn chạm ngưỡng đó thường xuyên, hàm `hashCode` của bạn có vấn đề, chứ không phải xui.\n\n## Load factor và chuyện resize\n\n`HashMap` mặc định `capacity = 16`, `loadFactor = 0.75`. Khi số phần tử vượt `16 * 0.75 = 12`, nó **nhân đôi** mảng và tính lại vị trí cho toàn bộ phần tử đang có.\n\nResize là thao tác đắt. Nếu biết trước sẽ nhét khoảng 1000 phần tử, hãy khai báo sẵn:\n\n```java\n// đừng để nó resize 7 lần\nMap<String, User> cache = new HashMap<>(1365);   // 1000 / 0.75 + 1\n```\n\n## Chỗ hay sai nhất: equals và hashCode\n\nĐây là lý do thật sự khiến bài này quan trọng.\n\n```java\npublic class OrderId {\n    private final String value;\n    // CHỈ override equals, quên hashCode\n    @Override public boolean equals(Object o) { ... }\n}\n\nMap<OrderId, Order> map = new HashMap<>();\nmap.put(new OrderId(\"A1\"), order);\nmap.get(new OrderId(\"A1\"));   // trả về null\n```\n\nVì sao null? `hashCode()` mặc định của `Object` dựa trên định danh đối tượng, nên hai `OrderId(\"A1\")` khác instance cho ra hai hash khác nhau, rơi vào hai bucket khác nhau. `equals` thậm chí không có cơ hội được gọi.\n\nQuy tắc chỉ có một dòng: **hai đối tượng bằng nhau thì bắt buộc phải cùng hashCode.** Chiều ngược lại không bắt buộc.\n\nVà một cái bẫy nữa: đừng dùng đối tượng **mutable** làm key. Sửa field sau khi đã `put` thì hash đổi, phần tử nằm nguyên ở bucket cũ và bạn không bao giờ tìm lại được nó.\n\n## Chọn loại Map nào\n\n| Loại | Thứ tự | Độ phức tạp | Dùng khi |\n|---|---|---|---|\n| `HashMap` | không có | O(1) trung bình | mặc định, 90% trường hợp |\n| `LinkedHashMap` | thứ tự chèn | O(1) | cần giữ thứ tự, làm LRU cache |\n| `TreeMap` | sắp theo key | O(log n) | cần duyệt theo thứ tự, tìm khoảng |\n| `ConcurrentHashMap` | không có | O(1) | nhiều luồng cùng ghi |\n\n> `HashMap` **không** an toàn với đa luồng. Hai luồng cùng resize từng gây vòng lặp vô hạn ở Java 7. Java 8 sửa chỗ đó nhưng vẫn có thể mất dữ liệu — cần đa luồng thì dùng `ConcurrentHashMap`.\n\n## Tóm lại\n\n- Vị trí được **tính ra**, không phải tìm kiếm — đó là lý do nó nhanh\n- Va chạm là bình thường; danh sách dài quá thì thành cây\n- Override `equals` thì phải override `hashCode`, không có ngoại lệ\n- Key nên là immutable\n"
  },
  {
    "id": "request-http-di-qua-dau",
    "title": "Một request HTTP đi qua những đâu",
    "date": "2026-07-22",
    "category": "Code",
    "topic": "Mạng máy tính",
    "tags": [
      "Mạng",
      "HTTP"
    ],
    "summary": "Từ lúc gõ URL tới lúc màn hình hiện nội dung, có khoảng bảy chặng. Biết chúng thì lúc chậm hay lỗi bạn biết phải mở chỗ nào ra xem.",
    "body": "Câu hỏi \"chuyện gì xảy ra khi bạn gõ một URL\" bị hỏi nhiều tới mức thành nhàm. Nhưng nó thật sự hữu ích: mỗi chặng trong đó là một chỗ có thể chậm, và biết chặng nào là biết mở công cụ nào.\n\n## Toàn cảnh\n\n```mermaid\nsequenceDiagram\n  autonumber\n  participant B as Trình duyệt\n  participant D as DNS\n  participant S as Server\n  B->>D: api.shop.vn là IP nào?\n  D-->>B: 203.0.113.10\n  B->>S: TCP SYN\n  S-->>B: SYN-ACK\n  B->>S: ACK\n  Note over B,S: bắt tay ba bước xong\n  B->>S: TLS ClientHello\n  S-->>B: Certificate + ServerHello\n  Note over B,S: kênh đã mã hoá\n  B->>S: GET /products/1\n  S-->>B: 200 OK + JSON\n```\n\n## Chặng 1 — DNS\n\nTên miền phải đổi thành địa chỉ IP. Trình duyệt hỏi lần lượt: cache của chính nó → cache hệ điều hành → DNS resolver của nhà mạng → root → TLD → name server của tên miền.\n\nLần đầu có thể mất 20–120ms. Những lần sau gần như bằng không nhờ cache, thời gian cache do bản ghi **TTL** quyết định.\n\n> Khi đổi IP server, TTL cũ vẫn còn hiệu lực với người dùng đã cache. Muốn chuyển mượt thì hạ TTL xuống vài phút **trước** ngày chuyển, chứ không phải lúc đang chuyển.\n\n## Chặng 2 — Bắt tay TCP\n\nBa bước: `SYN` → `SYN-ACK` → `ACK`. Chi phí đúng bằng **một vòng khứ hồi** (RTT). Máy chủ ở Singapore thì RTT từ Việt Nam khoảng 30–50ms; ở Mỹ thì 180–250ms.\n\n## Chặng 3 — Bắt tay TLS\n\nHTTPS thêm một lượt nữa để trao đổi chứng chỉ và thống nhất khoá. TLS 1.2 tốn 2 RTT, TLS 1.3 rút còn 1 RTT.\n\nCộng lại, một request HTTPS *đầu tiên* tới server ở Mỹ có thể mất gần một giây trước khi byte dữ liệu đầu tiên được gửi đi. Đây là lý do **keep-alive** quan trọng: giữ kết nối lại để những request sau bỏ qua chặng 2 và 3.\n\n```java\n// Java 11+ HttpClient tự dùng lại kết nối\nHttpClient client = HttpClient.newBuilder()\n        .version(HttpClient.Version.HTTP_2)\n        .connectTimeout(Duration.ofSeconds(5))\n        .build();\n```\n\nTạo `HttpClient` mới cho mỗi request là vứt bỏ toàn bộ lợi ích đó. Tạo một lần, dùng lại — điều này đúng với cả `RestTemplate` và `WebClient` của Spring.\n\n## Chặng 4 — Request và response\n\nMột request HTTP chỉ là văn bản có cấu trúc:\n\n```http\nGET /products/1 HTTP/1.1\nHost: api.shop.vn\nAccept: application/json\nAuthorization: Bearer eyJhbGci...\n```\n\nResponse cũng vậy, mở đầu bằng mã trạng thái:\n\n| Nhóm | Nghĩa | Ví dụ hay gặp |\n|---|---|---|\n| 2xx | thành công | 200 OK, 201 Created, 204 No Content |\n| 3xx | chuyển hướng | 301 vĩnh viễn, 302 tạm thời, 304 chưa đổi |\n| 4xx | **client sai** | 400, 401 chưa xác thực, 403 không đủ quyền, 404, 409 xung đột |\n| 5xx | **server sai** | 500, 502 gateway hỏng, 503 quá tải, 504 gateway hết giờ |\n\nPhân biệt 401 và 403 hay bị nhầm: **401 là \"anh là ai?\"**, **403 là \"biết anh rồi, nhưng không cho\"**.\n\n## Chỗ này giúp gì lúc gỡ lỗi\n\n- Chậm ở **DNS** → tab Network của trình duyệt, dòng \"DNS Lookup\"\n- Chậm ở **kết nối** → dòng \"Initial connection\" / \"SSL\"\n- Chậm ở **TTFB** (time to first byte) → lỗi nằm ở server, không phải mạng\n- **502/504** → server ứng dụng chết hoặc trả lời quá chậm, không phải lỗi người dùng\n\nMở tab Network, cột Waterfall chia sẵn theo đúng các chặng trên. Nhìn thanh nào dài nhất là biết phải đào chỗ nào.\n"
  },
  {
    "id": "index-tao-roi-van-cham",
    "title": "Đã tạo index rồi mà query vẫn chậm",
    "date": "2026-07-20",
    "category": "Code",
    "topic": "Database & SQL",
    "tags": [
      "SQL",
      "PostgreSQL",
      "Hiệu năng"
    ],
    "summary": "Index không phải cứ tạo là chạy. Có bốn kiểu viết câu lệnh khiến database bỏ qua index bạn vừa tạo, và EXPLAIN sẽ nói thẳng điều đó.",
    "body": "Tình huống quen thuộc: query chậm, thêm index, deploy, vẫn chậm y như cũ. Gần như luôn là một trong bốn nguyên nhân dưới đây.\n\n## Trước hết: index là cái gì\n\nHầu hết index trong PostgreSQL và MySQL là **B-Tree** — một cây cân bằng, các nút lá nối với nhau thành danh sách có thứ tự.\n\n```mermaid\nflowchart TD\n  R[\"gốc: 50\"] --> A[\"10 | 30\"]\n  R --> B[\"70 | 90\"]\n  A --> A1[\"1..9\"]\n  A --> A2[\"11..29\"]\n  A --> A3[\"31..49\"]\n  B --> B1[\"51..69\"]\n  B --> B2[\"71..89\"]\n  B --> B3[\"91..99\"]\n```\n\nTìm một giá trị chỉ mất vài bước nhảy thay vì quét toàn bảng. Quan trọng hơn: vì các lá **có thứ tự**, B-Tree cũng phục vụ được `ORDER BY`, `>`, `<`, `BETWEEN` và `LIKE 'abc%'` — nhưng **không** phục vụ được `LIKE '%abc'`.\n\n## Bẫy 1 — Bọc hàm quanh cột\n\n```sql\n-- index trên created_at bị bỏ qua\nSELECT * FROM orders WHERE DATE(created_at) = '2026-07-20';\n\n-- viết lại: cột đứng trần một mình\nSELECT * FROM orders\nWHERE created_at >= '2026-07-20' AND created_at < '2026-07-21';\n```\n\nIndex lưu giá trị của `created_at`, không lưu giá trị của `DATE(created_at)`. Khi cột bị bọc trong hàm, database không còn cách nào ánh xạ điều kiện về cây nữa. Thuật ngữ cho câu lệnh dùng được index là **sargable**.\n\nĐiều này đúng với mọi phép biến đổi: `UPPER(email) = ...`, `amount * 100 > ...`, `CAST(id AS text) = ...`.\n\n## Bẫy 2 — Sai thứ tự trong composite index\n\n```sql\nCREATE INDEX idx_orders ON orders (customer_id, status, created_at);\n```\n\nIndex nhiều cột hoạt động theo **leftmost prefix**: chỉ dùng được nếu điều kiện của bạn bắt đầu từ cột trái nhất và đi liên tiếp.\n\n| Điều kiện WHERE | Dùng được index? |\n|---|---|\n| `customer_id = 5` | có |\n| `customer_id = 5 AND status = 'NEW'` | có, cả hai cột |\n| `customer_id = 5 AND created_at > ...` | chỉ dùng được `customer_id` |\n| `status = 'NEW'` | **không** |\n| `status = 'NEW' AND created_at > ...` | **không** |\n\nHãy hình dung danh bạ điện thoại sắp theo (họ, tên). Tìm theo họ thì dễ; tìm mọi người tên \"Tùng\" bất kể họ thì vẫn phải lật hết cuốn.\n\n## Bẫy 3 — Lệch kiểu dữ liệu\n\n```sql\n-- user_id là bigint, tham số truyền vào là chuỗi\nSELECT * FROM orders WHERE user_id = '12345';\n```\n\nMột số database ép kiểu ngầm theo hướng làm hỏng index. Với JPA, chỗ này hay xảy ra khi entity khai `String` còn cột là `bigint`.\n\n## Bẫy 4 — Index có nhưng không đáng dùng\n\nNếu điều kiện khớp tới 30–40% số dòng, database sẽ **cố tình** quét toàn bảng, vì đọc tuần tự nhanh hơn nhảy ngẫu nhiên qua index rồi lại phải vào bảng lấy dữ liệu. Đây không phải lỗi — nó tính đúng.\n\nCách xử lý là **covering index**: đưa luôn cột cần đọc vào index để khỏi phải quay lại bảng.\n\n```sql\nCREATE INDEX idx_orders_cover ON orders (customer_id, status)\n  INCLUDE (total_amount, created_at);\n```\n\nKhi đó `EXPLAIN` sẽ hiện `Index Only Scan` — dấu hiệu tốt nhất bạn có thể thấy.\n\n## Đọc EXPLAIN\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM orders WHERE customer_id = 42 AND status = 'NEW';\n```\n\nChỉ cần nhớ bốn dòng này:\n\n- `Seq Scan` — quét toàn bảng. Trên bảng lớn là báo động.\n- `Index Scan` — dùng index rồi vào bảng lấy dữ liệu. Tốt.\n- `Index Only Scan` — lấy được hết từ index. Tốt nhất.\n- `rows=1000` mà `actual rows=900000` — thống kê lệch nặng, chạy `ANALYZE`.\n\n## Cái giá của index\n\nIndex không miễn phí. Mỗi `INSERT`, `UPDATE`, `DELETE` đều phải cập nhật toàn bộ index của bảng. Một bảng có 12 index thì ghi chậm thấy rõ.\n\n> Nguyên tắc tôi dùng: tạo index vì một câu query cụ thể đang chậm, đo lại, và xoá những index không còn ai dùng. `pg_stat_user_indexes` cho biết index nào chưa từng được quét lần nào.\n"
  },
  {
    "id": "cache-aside-va-ba-cai-bay",
    "title": "Cache-aside với Redis và ba cái bẫy",
    "date": "2026-07-18",
    "category": "Code",
    "topic": "Redis",
    "tags": [
      "Redis",
      "Cache",
      "Spring"
    ],
    "summary": "Đọc cache trước, miss thì xuống DB rồi ghi lại — nghe đơn giản, nhưng đúng ba chỗ sẽ làm sập database của bạn vào đúng giờ cao điểm.",
    "body": "**Cache-aside** (còn gọi là lazy loading) là kiểu cache phổ biến nhất và cũng là kiểu dễ làm sai nhất.\n\n## Luồng cơ bản\n\n```mermaid\nsequenceDiagram\n  participant A as Ứng dụng\n  participant R as Redis\n  participant D as Database\n  A->>R: GET product:1\n  alt cache hit\n    R-->>A: dữ liệu\n  else cache miss\n    R-->>A: (rỗng)\n    A->>D: SELECT * FROM product WHERE id=1\n    D-->>A: dữ liệu\n    A->>R: SET product:1 ttl=600\n    A-->>A: trả về\n  end\n```\n\n```java\npublic Product getProduct(Long id) {\n    String key = \"product:\" + id;\n    Product cached = redis.opsForValue().get(key);\n    if (cached != null) return cached;\n\n    Product p = repository.findById(id).orElseThrow();\n    redis.opsForValue().set(key, p, Duration.ofMinutes(10));\n    return p;\n}\n```\n\nChạy tốt trên máy bạn. Lên production thì gặp ba thứ sau.\n\n## Bẫy 1 — Cache stampede\n\nKey `product:1` của trang chủ hết hạn lúc 8h00. Ngay giây đó có 3000 request cùng vào, cả 3000 cùng thấy miss, cả 3000 cùng chạy xuống database.\n\nDatabase vốn chỉ phải chịu 1 query giờ nhận 3000 query giống hệt nhau. Nó chết, và khi nó chết thì cache cũng không bao giờ được ghi lại — vòng lặp tử thần.\n\nHai cách chữa, nên dùng cả hai:\n\n**Khoá phân tán** — chỉ cho một request đi xuống DB:\n\n```java\nBoolean gotLock = redis.opsForValue()\n        .setIfAbsent(\"lock:\" + key, \"1\", Duration.ofSeconds(3));\n\nif (Boolean.TRUE.equals(gotLock)) {\n    try {\n        Product p = repository.findById(id).orElseThrow();\n        redis.opsForValue().set(key, p, ttlWithJitter());\n        return p;\n    } finally {\n        redis.delete(\"lock:\" + key);\n    }\n}\n// những request còn lại chờ một nhịp rồi đọc lại cache\nThread.sleep(50);\nreturn getProduct(id);\n```\n\n**TTL có nhiễu** — đừng để hàng loạt key hết hạn cùng lúc:\n\n```java\nprivate Duration ttlWithJitter() {\n    // 10 phút ± tối đa 2 phút\n    long base = 600, jitter = ThreadLocalRandom.current().nextLong(-120, 121);\n    return Duration.ofSeconds(base + jitter);\n}\n```\n\n## Bẫy 2 — Cache penetration\n\nAi đó gọi liên tục `/products/999999999` — id không tồn tại. Cache luôn miss (vì không có gì để cache), database luôn phải chạy query. Cache trở thành vô dụng và DB lãnh trọn tải.\n\nCách chữa đơn giản nhất: **cache cả kết quả rỗng**, với TTL ngắn.\n\n```java\nOptional<Product> found = repository.findById(id);\nif (found.isEmpty()) {\n    redis.opsForValue().set(key, NULL_MARKER, Duration.ofSeconds(60));\n    throw new NotFoundException();\n}\n```\n\nVới lưu lượng lớn hơn thì dùng **Bloom filter** chặn từ đầu, nhưng 60 giây TTL cho giá trị rỗng đã giải quyết được phần lớn trường hợp.\n\n## Bẫy 3 — Cache avalanche\n\nRedis restart, hoặc bạn vừa `FLUSHALL`, hoặc toàn bộ key được nạp cùng lúc lúc khởi động nên hết hạn cùng lúc. Toàn bộ tải đổ xuống database trong một nhịp.\n\nTTL có nhiễu ở trên đã xử lý phần \"hết hạn cùng lúc\". Phần \"Redis chết\" thì cần **circuit breaker**: khi Redis không phản hồi, đừng để mỗi request chờ timeout 2 giây rồi mới xuống DB — hãy bỏ qua cache ngay và giới hạn số lượng đi xuống.\n\n## Xoá cache thế nào cho đúng\n\nKhi cập nhật dữ liệu, **xoá key chứ đừng ghi đè**:\n\n```java\n@Transactional\npublic void updatePrice(Long id, BigDecimal price) {\n    repository.updatePrice(id, price);\n    redis.delete(\"product:\" + id);   // xoá, không phải set\n}\n```\n\nLý do: hai request cập nhật song song có thể ghi đè lẫn nhau theo thứ tự sai, để lại giá trị cũ nằm trong cache vĩnh viễn. Xoá thì lần đọc kế tiếp tự nạp lại đúng.\n\nVà chú ý thứ tự với transaction: xoá cache **sau khi** commit. Nếu xoá trước mà transaction rollback, cache đã bị xoá trong khi DB vẫn giữ dữ liệu cũ — không sai lệch, chỉ tốn một lần miss. Ngược lại, xoá trước rồi có request khác nạp lại giá trị *cũ* vào cache trước khi bạn commit thì mới là hỏng thật.\n\n> Với Spring, `@CacheEvict` có thuộc tính `beforeInvocation = false` (mặc định) — tức là xoá sau khi phương thức chạy xong. Đúng như mong muốn.\n\n## Tóm tắt\n\n| Vấn đề | Triệu chứng | Cách chữa |\n|---|---|---|\n| Stampede | DB tăng vọt đúng lúc key hết hạn | khoá phân tán + TTL nhiễu |\n| Penetration | cache hit rate thấp bất thường | cache giá trị rỗng, Bloom filter |\n| Avalanche | DB sập sau khi Redis restart | TTL nhiễu + circuit breaker |\n"
  },
  {
    "id": "docker-multi-stage-va-layer",
    "title": "Multi-stage build và thứ tự layer trong Dockerfile",
    "date": "2026-07-16",
    "category": "Code",
    "topic": "Docker & CI/CD",
    "tags": [
      "Docker",
      "CI/CD",
      "Java"
    ],
    "summary": "Cùng một ứng dụng Spring Boot: 780MB hay 190MB, build lại mất 4 phút hay 20 giây — khác nhau ở thứ tự vài dòng lệnh.",
    "body": "Dockerfile đầu tiên của hầu hết dự án Java trông như thế này, và nó sai ở hai chỗ:\n\n```dockerfile\nFROM eclipse-temurin:21-jdk\nWORKDIR /app\nCOPY . .\nRUN ./mvnw clean package -DskipTests\nCMD [\"java\", \"-jar\", \"target/app.jar\"]\n```\n\nKết quả: image 780MB, và mỗi lần sửa một dòng code là build lại từ đầu mất 4 phút.\n\n## Hiểu layer trước\n\nMỗi lệnh `FROM`, `COPY`, `RUN`, `ADD` tạo ra một **layer**. Docker cache từng layer, và quy tắc chỉ có một: **một layer bị đổi thì toàn bộ layer phía sau nó mất cache.**\n\n```mermaid\nflowchart TD\n  L1[\"FROM temurin:21-jdk<br/>~450MB · gần như không đổi\"] --> L2\n  L2[\"COPY pom.xml<br/>đổi khi thêm thư viện\"] --> L3\n  L3[\"RUN mvn dependency:go-offline<br/>~180MB · tải thư viện\"] --> L4\n  L4[\"COPY src/<br/>đổi mỗi lần sửa code\"] --> L5\n  L5[\"RUN mvn package<br/>biên dịch\"]\n  style L4 stroke-width:3px\n  style L5 stroke-width:3px\n```\n\nTrong Dockerfile sai ở trên, `COPY . .` đứng trước `RUN ./mvnw package`. Sửa một ký tự trong code là `COPY` đổi, kéo theo bước tải toàn bộ thư viện Maven phải chạy lại. Đó là 3 phút rưỡi trong số 4 phút.\n\n## Sửa 1 — Tách phần ít đổi lên trên\n\n```dockerfile\nCOPY pom.xml mvnw ./\nCOPY .mvn .mvn\nRUN ./mvnw dependency:go-offline -B    # layer này cache lại được\n\nCOPY src ./src                          # chỉ layer này đổi khi sửa code\nRUN ./mvnw clean package -DskipTests\n```\n\nGiờ sửa code chỉ build lại hai layer cuối. Từ 4 phút xuống khoảng 40 giây.\n\n## Sửa 2 — Multi-stage\n\nImage cuối cùng đang chứa JDK đầy đủ, Maven, toàn bộ mã nguồn và thư mục `~/.m2` vài trăm MB. Chạy ứng dụng không cần thứ nào trong đó — chỉ cần JRE và một file jar.\n\n```dockerfile\n# ---------- giai đoạn build ----------\nFROM eclipse-temurin:21-jdk AS build\nWORKDIR /app\nCOPY pom.xml mvnw ./\nCOPY .mvn .mvn\nRUN ./mvnw dependency:go-offline -B\nCOPY src ./src\nRUN ./mvnw clean package -DskipTests\n\n# ---------- giai đoạn chạy ----------\nFROM eclipse-temurin:21-jre-alpine\nWORKDIR /app\nRUN addgroup -S app && adduser -S app -G app\nCOPY --from=build /app/target/*.jar app.jar\nUSER app\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"/app/app.jar\"]\n```\n\nChỉ những gì được `COPY --from=build` mới đi vào image cuối. Kết quả:\n\n| | Trước | Sau |\n|---|---|---|\n| Kích thước image | 780MB | 190MB |\n| Build lại khi sửa code | ~4 phút | ~40 giây |\n| Chạy bằng user root | có | không |\n\n## Đừng quên .dockerignore\n\nKhông có file này thì `COPY` sẽ nhét cả `target/`, `.git/`, `.idea/` vào build context — chậm và có khi lộ cả file cấu hình.\n\n```\ntarget/\n.git/\n.idea/\n*.iml\n.env\n**/node_modules\n```\n\n## Vài thứ nên có trong Dockerfile production\n\n**Không chạy bằng root.** Đã làm ở trên bằng `adduser`.\n\n**Ghim phiên bản.** `FROM eclipse-temurin:21-jre-alpine` tốt hơn `:latest`, nhưng ghim theo digest còn chắc hơn nữa.\n\n**Cho JVM biết nó đang ở trong container.** Từ JDK 10 trở lên, JVM tự đọc giới hạn cgroup, nhưng nên khai báo tường minh:\n\n```dockerfile\nENV JAVA_TOOL_OPTIONS=\"-XX:MaxRAMPercentage=75 -XX:+UseG1GC\"\n```\n\nĐừng đặt `-Xmx` cứng — dùng phần trăm để container đổi giới hạn RAM thì JVM tự theo.\n\n**Healthcheck** để orchestrator biết lúc nào ứng dụng thật sự sẵn sàng:\n\n```dockerfile\nHEALTHCHECK --interval=30s --timeout=3s --start-period=40s \\\n  CMD wget -qO- http://localhost:8080/actuator/health | grep -q UP\n```\n\n## Nối vào GitHub Actions\n\n```yaml\nname: build\non:\n  push:\n    branches: [main]\njobs:\n  docker:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/setup-buildx-action@v3\n      - uses: docker/build-push-action@v6\n        with:\n          push: true\n          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}\n          cache-from: type=gha\n          cache-to: type=gha,mode=max\n```\n\nHai dòng `cache-from` / `cache-to` là thứ giữ lại layer cache giữa các lần chạy CI. Không có chúng thì mọi tối ưu ở trên chỉ có tác dụng trên máy bạn.\n\n> Gắn tag bằng `github.sha` chứ đừng dùng `latest` cho deploy. Bạn cần biết chính xác commit nào đang chạy trên production lúc 2 giờ sáng.\n"
  },
  {
    "id": "kafka-rebalance-va-xu-ly-hai-lan",
    "title": "Kafka: consumer group, rebalance và message bị xử lý hai lần",
    "date": "2026-07-14",
    "category": "Code",
    "topic": "Kafka",
    "tags": [
      "Kafka",
      "Spring",
      "Hệ phân tán"
    ],
    "summary": "Kafka hứa at-least-once, nghĩa là sớm muộn bạn sẽ xử lý trùng một message. Bài này giải thích lúc nào chuyện đó xảy ra và cách làm consumer chịu được nó.",
    "body": "Ai dùng Kafka đủ lâu cũng gặp: một đơn hàng bị trừ tiền hai lần, một email gửi đi hai bản. Không phải Kafka hỏng — nó đang làm đúng thứ nó hứa.\n\n## Partition và consumer group\n\nMột topic chia thành nhiều **partition**. Thứ tự message chỉ được đảm bảo *trong một partition*, không phải trên toàn topic.\n\nMột **consumer group** là một nhóm consumer cùng chia nhau các partition. Quy tắc: **mỗi partition tại một thời điểm chỉ được gán cho đúng một consumer trong nhóm.**\n\n```mermaid\nflowchart LR\n  subgraph T[\"topic: orders — 4 partition\"]\n    P0[\"P0\"]\n    P1[\"P1\"]\n    P2[\"P2\"]\n    P3[\"P3\"]\n  end\n  subgraph G[\"consumer group: order-service\"]\n    C1[\"consumer 1\"]\n    C2[\"consumer 2\"]\n  end\n  P0 --> C1\n  P1 --> C1\n  P2 --> C2\n  P3 --> C2\n```\n\nHệ quả trực tiếp: **số consumer chạy song song không bao giờ vượt được số partition.** Có 4 partition mà chạy 6 pod thì 2 pod ngồi không. Muốn tăng thông lượng phải tăng partition trước.\n\n## Offset — chỗ mọi chuyện bắt đầu\n\nConsumer đọc tới đâu thì ghi lại vị trí đó, gọi là **commit offset**. Mặc định Spring Kafka commit tự động theo chu kỳ. Và đây là kịch bản sinh ra bản trùng:\n\n```mermaid\nsequenceDiagram\n  participant K as Kafka\n  participant C as Consumer\n  participant D as Database\n  K-->>C: message offset=100\n  C->>D: trừ tiền đơn hàng\n  D-->>C: xong\n  Note over C: pod bị kill trước khi commit\n  Note over K: offset vẫn đang là 100\n  K-->>C: giao lại offset=100 cho consumer khác\n  C->>D: trừ tiền LẦN HAI\n```\n\nCông việc đã làm xong nhưng offset chưa kịp ghi. Kafka không có cách nào biết điều đó, nên nó giao lại. Đó chính là **at-least-once**: không mất message, nhưng có thể lặp.\n\nĐổi sang commit *trước* khi xử lý thì thành **at-most-once** — hết trùng nhưng bắt đầu mất message. Thường tệ hơn nhiều.\n\n## Rebalance làm chuyện đó xảy ra thường xuyên hơn\n\n**Rebalance** là lúc Kafka chia lại partition cho nhóm. Nó xảy ra khi có consumer vào nhóm, rời nhóm, hoặc bị coi là đã chết. Trong lúc rebalance, cả nhóm dừng tiêu thụ.\n\nNguyên nhân hay gặp nhất không phải pod chết, mà là **xử lý quá lâu**:\n\n```properties\nmax.poll.interval.ms=300000   # 5 phút\nmax.poll.records=500\n```\n\nNếu một lần `poll()` lấy về 500 message, mỗi message xử lý 1 giây, thì vòng lặp mất 500 giây > 5 phút. Kafka kết luận consumer đã chết, đá nó ra khỏi nhóm và rebalance. Consumer kia thì vẫn đang xử lý bình thường, xử lý xong mới phát hiện mình bị đá — và toàn bộ lô message đó bị giao lại cho người khác.\n\nTriệu chứng: log đầy `Attempt to heartbeat failed since group is rebalancing`, và số lượng bản trùng tăng vọt.\n\nCách chữa: giảm `max.poll.records` cho khớp với tốc độ xử lý thật, hoặc tăng `max.poll.interval.ms`, hoặc đẩy phần việc nặng sang thread pool riêng.\n\n## Cách sống chung: consumer idempotent\n\nĐừng cố làm cho message không bao giờ lặp. Hãy làm cho **xử lý lặp không gây hại**.\n\nCách chắc chắn nhất là bảng khử trùng, ghi trong cùng transaction với nghiệp vụ:\n\n```sql\nCREATE TABLE processed_message (\n    message_key   VARCHAR(120) PRIMARY KEY,\n    processed_at  TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n```\n\n```java\n@KafkaListener(topics = \"orders\", groupId = \"order-service\")\n@Transactional\npublic void onMessage(ConsumerRecord<String, OrderEvent> record) {\n    String key = record.topic() + \"-\" + record.partition() + \"-\" + record.offset();\n    try {\n        processedRepo.insert(key);       // khoá chính chặn bản trùng\n    } catch (DuplicateKeyException e) {\n        return;                          // đã xử lý rồi, bỏ qua\n    }\n    orderService.charge(record.value());\n}\n```\n\nĐiểm mấu chốt: `insert` và `charge` phải nằm **trong cùng một transaction database**. Nếu tách ra, bạn chỉ đẩy vấn đề sang chỗ khác.\n\nNếu nghiệp vụ có sẵn khoá tự nhiên (`order_id`, `payment_id`) thì dùng luôn nó làm khoá khử trùng, tốt hơn dùng offset — vì cùng một đơn hàng có thể được gửi lại với offset khác.\n\n## Commit thủ công cho rõ ràng\n\n```java\n@Bean\npublic ConcurrentKafkaListenerContainerFactory<String, Object> factory(...) {\n    var f = new ConcurrentKafkaListenerContainerFactory<String, Object>();\n    f.getContainerProperties().setAckMode(ContainerProperties.AckMode.MANUAL);\n    return f;\n}\n\n@KafkaListener(topics = \"orders\")\npublic void onMessage(OrderEvent event, Acknowledgment ack) {\n    handle(event);\n    ack.acknowledge();     // commit sau khi xử lý xong\n}\n```\n\nVẫn là at-least-once, nhưng cửa sổ sinh bản trùng hẹp hơn nhiều và bạn kiểm soát được nó.\n\n## Message hỏng thì sao\n\nMột message không bao giờ xử lý được sẽ kẹt vĩnh viễn: retry, lỗi, retry, lỗi — cả partition đứng lại. Cần **dead letter topic**:\n\n```java\n@Bean\npublic DefaultErrorHandler errorHandler(KafkaTemplate<String, Object> template) {\n    var recoverer = new DeadLetterPublishingRecoverer(template);\n    // thử lại 3 lần, giãn cách tăng dần, rồi đẩy sang orders.DLT\n    return new DefaultErrorHandler(recoverer, new ExponentialBackOff(1000L, 2.0));\n}\n```\n\nNhớ dựng cảnh báo trên số lượng message trong DLT. Một DLT không ai theo dõi thì cũng như xoá message đi thôi.\n\n## Bốn điều nên nhớ\n\n1. Song song tối đa = số partition\n2. At-least-once là mặc định — thiết kế consumer chịu được bản trùng\n3. Rebalance thường do xử lý chậm, không phải do pod chết\n4. Bảng khử trùng + transaction là cách rẻ và chắc nhất\n"
  },
  {
    "id": "p99-chu-khong-phai-trung-binh",
    "title": "Đo hiệu năng: p99 chứ không phải trung bình",
    "date": "2026-07-12",
    "category": "Code",
    "topic": "Hiệu năng",
    "tags": [
      "Hiệu năng",
      "Java",
      "Quan trắc"
    ],
    "summary": "Trung bình 120ms nghe rất ổn, cho tới khi bạn nhận ra 1% người dùng đang chờ 4 giây. Bài này về cách đo đúng và tìm đúng chỗ nghẽn.",
    "body": "Có một câu tôi nhắc mình mỗi lần định tối ưu: **đo trước, đoán sau.** Phần lớn thời gian tôi đoán sai chỗ nghẽn, và code tôi \"tối ưu\" chỉ làm nó khó đọc hơn.\n\n## Vì sao trung bình luôn nói dối\n\nGiả sử 100 request:\n\n- 99 request: 100ms\n- 1 request: 4000ms\n\nTrung bình = `(99 × 100 + 4000) / 100 = 139ms`. Nhìn dashboard thấy 139ms, kết luận hệ thống khoẻ.\n\nNhưng người dùng thứ 100 đó chờ 4 giây. Nếu một trang gọi 20 API nội bộ, xác suất *ít nhất một* request rơi vào nhóm chậm là `1 - 0.99^20 ≈ 18%`. Gần một phần năm số lần tải trang bị chậm — trong khi dashboard vẫn xanh.\n\n| Chỉ số | Nghĩa | Dùng để |\n|---|---|---|\n| p50 (trung vị) | nửa số request nhanh hơn mức này | cảm nhận thông thường |\n| p95 | 5% chậm hơn mức này | ngưỡng cảnh báo |\n| **p99** | 1% chậm hơn mức này | **cam kết SLO** |\n| p99.9 | 1 phần nghìn | hệ thống lớn, nhiều lượt gọi |\n| max | request tệ nhất | tìm ngoại lệ, không dùng để cảnh báo |\n\n> Không bao giờ cộng hay lấy trung bình các percentile của nhiều máy. p99 của ba pod cộng lại chia ba **không phải** là p99 của hệ thống. Muốn đúng thì phải gộp histogram, và đó chính là việc Prometheus `histogram_quantile` làm.\n\n## Đo trong ứng dụng\n\nSpring Boot Actuator + Micrometer cho sẵn histogram:\n\n```properties\nmanagement.endpoints.web.exposure.include=health,metrics,prometheus\nmanagement.metrics.distribution.percentiles-histogram.http.server.requests=true\nmanagement.metrics.distribution.slo.http.server.requests=50ms,100ms,200ms,500ms,1s\n```\n\n```java\n@Timed(value = \"order.create\", percentiles = {0.5, 0.95, 0.99})\npublic Order create(OrderRequest req) { ... }\n```\n\nTruy vấn Prometheus cho p99 trong 5 phút gần nhất:\n\n```\nhistogram_quantile(0.99,\n  sum(rate(http_server_requests_seconds_bucket[5m])) by (le, uri))\n```\n\n## Bốn nguyên nhân chậm hay gặp nhất\n\nTrước khi mở profiler, kiểm tra bốn thứ này — chúng chiếm phần lớn các ca tôi từng gặp:\n\n**1. N+1 query.** Lấy 100 đơn hàng rồi lặp qua từng đơn để lấy khách hàng: 101 lượt đi database. Bật log để nhìn thấy nó:\n\n```properties\nspring.jpa.properties.hibernate.generate_statistics=true\nlogging.level.org.hibernate.stat=DEBUG\n```\n\nChữa bằng `JOIN FETCH` hoặc `@EntityGraph`.\n\n**2. Connection pool quá nhỏ.** `HikariCP` mặc định 10 kết nối. Request thứ 11 xếp hàng chờ, và thời gian chờ đó nằm gọn trong p99. Dấu hiệu: `HikariPool-1 - Connection is not available, request timed out after 30000ms`.\n\nCông thức xuất phát: `connections = (số core × 2) + số đĩa`. Tăng lên 200 thường làm mọi thứ *chậm hơn* vì database phải chuyển ngữ cảnh nhiều hơn.\n\n**3. Gọi HTTP không đặt timeout.** Một dịch vụ phụ thuộc treo 30 giây sẽ kéo p99 của bạn lên 30 giây. Luôn đặt timeout, và đặt ngắn hơn timeout của người gọi bạn.\n\n**4. GC pause.** Nếu p99 dựng đứng theo chu kỳ đều đặn, khả năng cao là GC. Bật log để xác nhận:\n\n```\n-Xlog:gc*:file=/var/log/gc.log:time,uptime:filecount=5,filesize=10M\n```\n\n## Khi bốn thứ trên không phải nguyên nhân\n\nLúc đó mới đến profiler. **async-profiler** là công cụ tôi dùng, vì nó lấy mẫu được cả stack Java lẫn native mà không bóp méo kết quả:\n\n```bash\n# gắn vào tiến trình đang chạy, lấy mẫu CPU 30 giây, xuất flame graph\n./profiler.sh -d 30 -e cpu -f /tmp/cpu.html <pid>\n\n# đổi -e alloc để tìm chỗ cấp phát bộ nhớ nhiều\n./profiler.sh -d 30 -e alloc -f /tmp/alloc.html <pid>\n```\n\nCách đọc **flame graph**: trục ngang là *tỷ lệ thời gian*, không phải thời gian trôi qua. Tìm các mảng **rộng** — đó là chỗ tốn CPU. Chiều cao chỉ là độ sâu ngăn xếp, không có ý nghĩa về hiệu năng.\n\nNếu không cài thêm được gì, **JDK Flight Recorder** đã có sẵn trong JDK:\n\n```bash\njava -XX:StartFlightRecording=duration=60s,filename=app.jfr -jar app.jar\n```\n\nMở file `.jfr` bằng JDK Mission Control.\n\n## Thứ tự tôi hay làm\n\n```mermaid\nflowchart TD\n  A[\"p99 vượt ngưỡng\"] --> B{\"chậm đều hay<br/>chậm theo đợt?\"}\n  B -- theo đợt --> C[\"nghi GC hoặc rebalance<br/>xem log GC\"]\n  B -- đều --> D{\"thời gian nằm ở đâu?\"}\n  D --> E[\"database<br/>log query chậm, EXPLAIN\"]\n  D --> F[\"gọi dịch vụ ngoài<br/>kiểm tra timeout\"]\n  D --> G[\"trong CPU ứng dụng<br/>async-profiler\"]\n  E --> H[\"sửa, đo lại, so sánh\"]\n  F --> H\n  G --> H\n  C --> H\n```\n\nBước cuối cùng quan trọng nhất và hay bị bỏ: **đo lại**. Không có con số trước và sau thì bạn không biết mình đã sửa được gì, hay chỉ vừa đổi chỗ nghẽn sang một nơi khác.\n\n> Một lần tôi tối ưu một hàm parse mất hai ngày, giảm được 8ms trên p99. Sau đó phát hiện ra một `Thread.sleep(200)` ai đó để lại trong đoạn mã thử nghiệm từ năm trước. Đo trước, luôn luôn.\n"
  }
];
